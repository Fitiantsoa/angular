import { CommandeProduitKey } from './commande-produit-key';

describe('CommandeProduitKey', () => {
  it('should create an instance', () => {
    expect(new CommandeProduitKey()).toBeTruthy();
  });
});
